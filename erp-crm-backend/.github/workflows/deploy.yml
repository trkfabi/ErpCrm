name: Deploy to VPS

on:
  push:
    branches:
      - master # O la rama que desees monitorear

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Deploy to VPS
        run: |
          ssh -o StrictHostKeyChecking=no root@145.223.120.220 << 'EOF'
            cd /srv/node/ErpCrm
            git pull origin master
            docker stop erpcrm-app-container || true
            docker rm erpcrm-app-container || true
            docker build -t erpcrm-app .
            docker run -d -p 5000:3000 --name erpcrm-app-container erpcrm-app
          EOF
